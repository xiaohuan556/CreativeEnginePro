# CreativeEnginePro 产品简介

> 一款面向**短视频 / 信息流创作者**的桌面端一体化创作工具，集**视频批量剪辑、图片处理、视频混剪、图片轮播、AI 配音、AI 脚本、专业图片编辑、热点雷达**于一体。
> 定位：把过去需要多款软件（PR / PS / 剪映 / 配音工具 / 热点工具）完成的流水线，收拢到一个本地桌面应用里完成。

---

## 1. 它能做什么（功能总览）

软件采用**左侧导航 + 右侧 QStackedWidget 多模块**的布局，共 8 个一级工作区：

| 序号 | 工作区 | 核心功能 |
|------|--------|----------|
| 0 | **尾页处理**（视频处理） | 批量导入视频 → AI 智能分析断点 → 手动微调剪切点 → 批量导出 |
| 1 | **图片处理** | 批量导入图片 → 自定义尺寸/格式变换 → 批量导出 |
| 2 | **视频混剪** | 多素材随机/固定组合 → 多种混剪模式 → 批量生成差异化视频 |
| 3 | **图片轮播** | 图片自动轮播成片，内置 15 种转场（淡入淡出、滑动、3D 等） |
| 4 | **语音配音** | TTS 多引擎配音、语音克隆、批量配音、配音结果推送至剪辑素材库 |
| 5 | **AI 脚本** | AI 生成口播/文案脚本，与配音、剪辑工作台联动 |
| 6 | **剪辑工作台** | 多轨时间线、逐帧预览、字幕/关键帧、转场、绿幕抠像、导出 |
| 7 | **图片工作台** | 类 Photoshop 的图层/画板编辑（选区、吸管、渐变、形状、文本、AI 抠图、PSD 导入） |

此外，**热点雷达**作为聚合模块内嵌于主程序，提供多平台热点聚合、评分排序、配置化抓取与后台定时刷新。

---

## 2. 各模块功能细节

### 2.1 剪辑工作台（核心，Tab 6）
- **多轨时间线**：主轨 + 叠加轨，片段拖拽、分割、删除、吸附对齐。
- **逐帧预览**：后台线程取帧 → 帧缓存环 → 主线程合成，拖动不重解码、不闪烁。
- **字幕系统**：字幕块支持位置/缩放/旋转、关键帧动画、**逐词淡入**、ASR 自动生成字幕并带逐词动画。
- **关键帧**：对位置/缩放/旋转做线性插值；拖动片段实时跳过关键帧插值，释放时同步关键帧新值。
- **转场**：复用 `core/compositor.py` 逐帧合成，导出含转场。
- **绿幕抠像**：`utils/chroma_key.py` 双版本（numpy 后台线程版 / QImage 导出版），统一合成顺序保证透明通道正确。
- **Alpha 视频 / 解码器**：`core/clip_decoder.py` 单 `VideoCapture` + 环形缓冲 + `DecoderManager` 同文件多轨独立解码，避免共享 cap 位置错乱导致的卡顿。
- **撤销/重做**：pre-state 历史模型，每次操作独立可撤销（`Ctrl+Z`）。

### 2.2 图片工作台（Tab 7，类 PS）
- **图层系统**：图像/文本/形状/渐变/调整图层，变换手柄、智能参考线、对齐吸附。
- **画板（Artboard）**：多画板自由摆放，独立透明背景，画板边框 + 名称标签；画布随画板实时扩大、不被裁切。
- **选区工具**：矩形/椭圆/套索/多边形套索/魔棒/快速选择；羽化、布尔运算。
- **吸管**：专属吸管光标 + 实时取色小标志（HUD 跟随光标，透明处红斜杠提示）。
- **渐变**：专属「线段」光标 + 拖拽实时线段预览（起止色圆点 + 连线 + 角度/径向标注）。
- **AI 能力**：rembg / BiRefNet / CodeFormer 抠图与修复（干净子进程推理，规避主进程 DLL 冲突）。
- **PSD 导入**：`psd-tools` 解析多图层。
- **画布参考**：标尺、网格、参考线、旋转角度 HUD。

### 2.3 视频混剪 / 尾页 / 轮播
- **混剪引擎**（`core/mix_engine.py`、`core/mixer.py`）：素材池随机/固定组合、多种成片模式、批量差异化输出。
- **轮播引擎**（`core/slideshow_engine.py`）：15 种转场库 + `apply_transition`，可批量生成图集视频。
- **尾页处理**：AI 断点分析（基于镜头/运动检测）辅助批量剪辑。

### 2.4 语音配音（Tab 4）
- **多 TTS 引擎**：Edge-TTS（内置）、Fish、SiliconFlow、Deepgram、以及本地 `openai-whisper` ASR（`core/tts_*`、`core/whisper_runner.py`）。
- 语音克隆、批量配音、导出推送至剪辑素材库。
- 音频工具：`pydub` / `librosa` / `soundfile` 做切片、降噪、格式转换。

### 2.5 AI 脚本（Tab 5）
- 基于 LLM（`openai` 客户端）生成口播/带货/解说文案，与配音、剪辑工作台通过信号联动（`script_to_polish` / `polished_text`）。

### 2.6 热点雷达
- `ui/hotspot_handler.py`（`HotspotHandler` 混入 `UltimateEngine`）：并发抓取多源（含 X/TikTok 真实数据源）、评分排序、配置化、后台定时刷新、本地缓存秒开。

### 2.7 合规素材
- `tools/tribeofnoise_cc_downloader.py`：Tribe of Noise 免费 CC BY-SA 4.0 音乐下载器，自动生成署名表 `credits.csv`，不接入版权音乐 API。

---

## 3. 技术栈与代码结构

### 3.1 技术栈
| 类别 | 选型 |
|------|------|
| 语言 | Python 3.13 |
| GUI | PyQt6 + PyQt6-Multimedia |
| 视频 | FFmpeg（内嵌 `ffmpeg.exe`）、OpenCV（`opencv-contrib-python`）、MoviePy |
| 音频 | `pydub`、`librosa`、`soundfile`、Edge-TTS、`openai-whisper` |
| AI | `onnxruntime` + `rembg` / BiRefNet / CodeFormer / Real-ESRGAN；`openai` 客户端 |
| 工程化 | PyInstaller 单文件打包（含 `hooks/` 解决 onnxruntime/rembg 的原生 DLL 冲突） |

### 3.2 目录结构
```
CreativeEnginePro/
├── main.py                 # 入口；EXE 内 AI 子进程 worker 引导（--realesr-worker / --ai-worker）
├── config.py               # 全局配置
├── ui/                     # 界面层（PyQt6）
│   ├── main_window.py      # UltimateEngine 主窗口，多模块堆叠 + 侧边导航
│   ├── editor_tab.py       # 剪辑工作台（多轨/字幕/属性面板）
│   ├── preview_player.py   # 画布预览、帧合成、关键帧、字幕叠加、拖拽交互
│   ├── timeline_widget.py  # 多轨时间线、拖拽、播放时钟
│   ├── clip_properties.py  # 片段/字幕属性面板
│   ├── image_editor.py     # 图片工作台（图层/画板/选区/吸管/渐变/AI）
│   ├── voice_workbench.py  # 语音配音
│   ├── script_workbench.py # AI 脚本
│   ├── slideshow_handler.py# 图片轮播
│   ├── mix_handler.py      # 视频混剪
│   ├── hotspot_handler.py  # 热点雷达（混入主窗口）
│   ├── media_library.py / media_preview.py
│   ├── export_dialogs.py   # 导出设置
│   └── widgets/ workers/   # 复用组件与后台任务
├── core/                   # 业务核心（与界面解耦）
│   ├── edit_engine.py      # 多轨数据模型、撤销/重做、关键帧插值
│   ├── clip_decoder.py     # 单文件解码器 + DecoderManager
│   ├── compositor.py       # 逐帧合成（转场/绿幕/Alpha）
│   ├── slideshow_engine.py # 轮播转场库
│   ├── mix_engine.py / mixer.py / pipeline.py
│   ├── transcriber.py / whisper_runner.py / tts_*.py / tts_factory.py
│   ├── script_gen.py / builtin_translator.py / time_sync.py
│   └── plugins/            # 可扩展插件
├── utils/                  # 工具函数
│   ├── chroma_key.py       # 绿幕抠像（numpy / QImage 双版本）
│   ├── alpha_video.py      # Alpha 视频提取（BGRA）
│   ├── ffmpeg_utils.py / mask.py / logger.py
├── tools/                  # 独立小工具
│   └── tribeofnoise_cc_downloader.py  # CC 合规音乐下载
├── hooks/                  # PyInstaller 钩子（onnxruntime 干净导入）
├── assets/ / Cache/ / work_output/ / work_temp/
├── CreativeEnginePro.spec / main.spec  # 打包配置
└── 更新日志.md / 操作手册.md / AI_ASSET_STUDIO_PLAN.md
```

### 3.3 关键工程亮点
1. **解码器架构**：`ClipDecoder` 单 `VideoCapture` + 24 帧环形缓冲 + 状态机；`DecoderManager` 按 `source_path` 一对一映射；同文件多轨道用独立解码器，避免 `_ensure_forward` 假设被破坏导致卡顿。
2. **预览零闪烁**：拖拽/缩放/旋转期间走「快速路径」复用冻结源帧（`_compose_from_raw`），跳过关键帧插值；仅交互结束后执行完整合成。
3. **字幕无残影**：用 `_subtitle_base_canvas()` 干净无字幕底图 + `_last_frame_no_subs`，杜绝旧字幕叠加残留。
4. **AI 推理隔离**：主进程已加载 PyQt6/cv2 后 onnxruntime 原生 DLL 会初始化失败，故 AI 推理走 `--ai-worker` / `--realesr-worker` **干净子进程**（.npy 传数据、argv 传路径），打包版用 `datas` 内置进程内 import。
5. **撤销模型**：pre-state 历史（每次操作前快照），可实现细粒度逐步撤销/重做。
6. **热点聚合**：`ThreadPoolExecutor` 并发抓取 + 本地 `hotspot_cache.json` 秒开 + 后台定时刷新 + 评分排序（热度/互动对数归一）。
7. **合规优先**：素材下载只走 CC0/CC BY-SA 来源，拒绝接入版权音乐 API。

---

## 4. 如何运行与打包

### 运行（源码）
```bash
python main.py
```
> 注意：源码模式 AI 推理走干净子进程，功能最完整。

### 打包（单文件 EXE）
- 使用 `CreativeEnginePro.spec` + `hooks/` + `一键打包.bat`。
- 关键坑：onnxruntime / rembg 的 `.pyd` 在 PyInstaller 分析子进程里会 `access violation`，需整体 `datas` 拷贝 + 运行时 hook 加入 `os.add_dll_directory()` + 修补 `importlib.metadata`，并在主进程通过子进程做 AI 推理。

### 系统要求
- Windows（当前工程在 win32 环境开发）。
- FFmpeg 已随包内嵌（`ffmpeg.exe`）。
- 依赖见 `requirements.txt`（PyQt6、OpenCV、MoviePy、edge-tts、openai-whisper、rembg、psd-tools 等）。

---

## 5. 适用人群

- **短视频 / 信息流创作者**：批量产出差异化成片、字幕口播、热点跟拍。
- **电商 / 带货团队**：图文轮播、混剪、配音、脚本一体化。
- **剪辑工程师**：本地化、可私有部署、不依赖云端，素材版权合规可控。

---

> 本文档基于 `CreativeEnginePro` 工程结构（`ui/`、`core/`、`utils/`、`tools/`、`main.py`）与 `requirements.txt`、`更新日志.md` 整理。具体行为以源码与《操作手册》为准。
