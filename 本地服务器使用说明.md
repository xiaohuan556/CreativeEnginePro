# 本地电脑作为服务器

该方案适合临时试用：账号、任务记录和生成素材都保存在这台电脑的
`local_server_data` 目录。电脑关机、休眠、断网或局域网 IP 变化后，其他人将无法使用。

## 第一次使用

1. 双击 `创建本地管理员.bat`，按提示设置管理员账号和至少 12 位的密码。
2. 双击 `启动本地服务器.bat`。
3. 右键或双击 `开启局域网访问（管理员）.bat`，确认 Windows 的管理员提示。
4. 打开桌面端 EXE，在登录界面输入管理员账号和密码，点击“管理员账号管理”即可新增、停用和调整其他账号。
5. 普通用户直接在同一个桌面端登录界面登录。给其他人的 EXE 必须使用当前局域网地址打包，本机当前地址是 `10.13.12.67`。

不用时双击 `停止本地服务器.bat`。日志、数据库和媒体位于 `local_server_data`。

## 使用范围

- 仅供同一个可信 Wi-Fi/有线局域网使用。
- HTTP 登录信息没有公网级加密保护，绝对不要在路由器上把 8000 端口映射到互联网。
- 外地用户需要等以后部署 HTTPS 云服务器，或配置具有固定域名的安全隧道后重新打包。
- 这套启动脚本只服务桌面端，不会启动或修改 CreativeEnginePro Web 产品。
