# CreativeEnginePro 操作手册

> 信息流批量剪辑工具 — 集视频处理、图片处理、视频混剪于一体

---

## 目录

1. [软件概述](#1-软件概述)
2. [系统要求与安装](#2-系统要求与安装)
3. [项目结构](#3-项目结构)
4. [启动与打包](#4-启动与打包)
5. [界面布局总览](#5-界面布局总览)
6. [模块一：视频处理](#6-模块一视频处理)
7. [模块二：图片处理](#7-模块二图片处理)
8. [模块三：视频混剪](#8-模块三视频混剪)
9. [快捷键与操作技巧](#9-快捷键与操作技巧)
10. [常见问题排查](#10-常见问题排查)
11. [开发者指南](#11-开发者指南)

---

## 1. 软件概述

CreativeEnginePro 是一款面向短视频/信息流创作者的桌面批量剪辑工具，核心能力：

| 功能 | 说明 |
|------|------|
| **视频处理** | 批量导入视频 → AI智能分析断点 → 手动微调剪切点 → 批量导出 |
| **图片处理** | 批量导入图片 → 自定义尺寸变换 → 批量导出 |
| **视频混剪** | 多素材随机/固定组合 → 多种混剪模式 → 批量生成差异化视频 |

技术栈：Python 3 + PyQt6 + FFmpeg + MoviePy + OpenCV

---

## 2. 系统要求与安装

### 运行环境

- **操作系统**：Windows 10/11（64位）
- **Python**：3.9+（开发模式需要）
- **FFmpeg**：已内置（ffmpeg.exe，约213MB）

### 开发模式运行

```bash
cd C:\Users\A\Desktop\CreativeEnginePro
pip install PyQt6 moviepy opencv-python scikit-image imageio numpy scipy Pillow tqdm
python main.py
```

### 打包为单文件 EXE

双击 `一键打包.bat`，或手动执行：

```bash
python -m PyInstaller --clean main.spec
```

打包产物位于 `dist/CreativeEnginePro.exe`（单文件，约270MB）。

> **注意**：首次启动打包后的 EXE 需要等待 10-30 秒解压临时文件，属正常现象。

---

## 3. 项目结构

```
CreativeEnginePro/
├── main.py                  # 入口文件（授权检查 + 全局异常捕获 + 启动主窗口）
├── main.spec                # PyInstaller 打包配置
├── 一键打包.bat              # 一键打包脚本
├── ffmpeg.exe               # FFmpeg 可执行文件（视频/音频处理核心）
├── logo.ico                 # 应用图标
│
├── core/                    # 核心引擎层
│   ├── video_engine.py      # 视频处理引擎（FFmpeg + MoviePy 渲染）
│   ├── mix_engine.py        # 混剪引擎（任务生成 + FFmpeg 混合渲染）
│   └── image_engine.py      # 图片引擎（空文件，逻辑在 ui/image_handler.py）
│
├── ui/                      # 界面层
│   ├── main_window.py       # 主窗口（视频处理模块 + 侧边栏导航 + 全局控制）
│   ├── mix_handler.py       # 混剪模块界面（素材库 + 预览 + 参数配置）
│   └── image_handler.py     # 图片模块界面（导入 + 预览对比 + 批量导出）
│
├── utils/
│   └── ffmpeg_utils.py      # FFmpeg 路径工具（自动识别开发/打包环境）
│
├── assets/
│   └── icon.ico             # 资源图标
│
├── build/                   # PyInstaller 构建临时文件（可删除）
└── dist/                    # 打包输出目录
```

### 关键文件说明

| 文件 | 大小 | 职责 |
|------|------|------|
| `main_window.py` | ~61KB | 主窗口类 UltimateEngine，包含视频处理完整 UI、表格管理、多线程任务调度、撤销系统 |
| `video_engine.py` | ~24KB | VideoProcessor 类，两种渲染路径：fast_remux（FFmpeg 直出）和 full_render（MoviePy 全量渲染） |
| `mix_handler.py` | ~41KB | MixHandler 混剪模块，三栏布局，5种混剪模式 |
| `mix_engine.py` | ~14KB | ClipMaterial 数据结构 + 5种 MixMode + TaskGenerator 防重复任务生成 + FFmpegMixer 渲染 |
| `image_handler.py` | ~32KB | ImageHandler 图片模块，3种变换策略，导入/预览/批量导出 |

---

## 4. 启动与打包

### 启动流程

1. `main.py` 执行路径兼容处理（支持 PyInstaller 打包环境）
2. 绑定全局异常处理器（防闪退，未捕获异常弹窗显示详情）
3. 检查授权过期（硬编码到期日 2026-06-01，过期弹窗退出）
4. 创建 QApplication，设置字体 Segoe UI
5. 实例化 UltimateEngine 主窗口并显示

### 打包说明

- **打包模式**：单文件 EXE（`--onefile`）
- **FFmpeg 内嵌**：ffmpeg.exe 作为 binary 打包进 EXE
- **运行时解压**：启动时解压到系统临时目录 `_MEIPASS`
- **图标**：logo.ico
- **无控制台**：`console=False`，不显示命令行窗口

---

## 5. 界面布局总览

```
┌─────────────────────────────────────────────────────┐
│  小欢ovo | 信息流批量剪辑工具            [_][□][×]   │
├──────────┬──────────────────────────────────────────┤
│          │                                          │
│ 侧边栏    │              主工作区                     │
│          │                                          │
│ ○ 视频处理 │   根据侧边栏切换显示:                     │
│ ○ 图片处理 │   - 视频处理模块                          │
│ ○ 视频混剪 │   - 图片处理模块                          │
│          │   - 视频混剪模块                          │
│          │                                          │
├──────────┴──────────────────────────────────────────┤
│  全局配色：深色主题 #1e1e1e 底色 / #3d8ef8 蓝色强调 /   │
│  #00eaff 青色高亮 / #e74c3c 红色危险按钮               │
└─────────────────────────────────────────────────────┘
```

**侧边栏**：3个导航按钮切换 QStackedWidget 中的模块页面。

---

## 6. 模块一：视频处理

### 6.1 界面布局

```
┌──────────────────────────────────────────────────────────┐
│ ┌─ 左侧（视频队列表格）──┐  ┌─ 右侧（控制面板）────────┐ │
│ │ ☑ │ 文件名 │ 原比例 │ AI │  │  虚化/裁切预览画布       │ │
│ │ ☑ │ v1.mp4│ 9:16  │ ✓ │  │  ┌──────────────────┐    │ │
│ │ ☐ │ v2.mp4│ 16:9  │ -- │  │  │  SmartTimeline    │    │ │
│ │ ☑ │ v3.mp4│ 9:16  │ ✓ │  │  │  拖拽剪切点 │   │    │ │
│ │   │       │       │    │  │  └──────────────────┘    │ │
│ │                         │  │                          │ │
│ │ [全选/反选] [智能解析]    │  │  输出参数设定             │ │
│ │ [清空列表] [删除选中]     │  │  尺寸适配: [下拉选择]     │ │
│ │                         │  │  统一命名: [_______]      │ │
│ │                         │  │  替换尾页: [选择文件]      │ │
│ │                         │  │                          │ │
│ │                         │  │  控制台日志               │ │
│ │                         │  │  ☑ 导出后自动打开文件夹    │ │
│ │                         │  │  [进度条]                 │ │
│ │                         │  │  [仅导出当前] [同步比例]   │ │
│ │                         │  │  [批量导出] [停止输出]     │ │
│ └─────────────────────────┘  └──────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

### 6.2 完整操作流程

#### 第一步：导入视频

- **拖拽导入**：直接将视频文件拖入窗口任意位置
- **按钮导入**：点击"添加视频"按钮选择文件
- 自动检测重复视频（基于文件路径去重）
- 导入后表格显示：勾选框 | 文件名 | 原始比例 | AI状态

#### 第二步：智能解析断点

1. 选中一行视频（点击高亮）
2. 点击 **"智能解析所有断点"** 按钮
   - 支持批量解析（多核并发 ThreadPoolExecutor）
   - 解析进度实时显示在表格第3列（处理中 → 已完成/已取消/失败）
3. AI 分析逻辑：
   - 顺序解码视频帧（只解码一次，缓存到内存）
   - 倒序分析末尾画面变化
   - 使用 SSIM + 亮度检测三种模式：
     - **黑场检测**：亮度 < 8.0，置信度 95%
     - **白闪检测**：亮度 > 230，置信度 79%
     - **SSIM 突变**：SSIM < 0.45，置信度 75%
4. 解析完成后，右侧预览画布显示裁切预览，SmartTimeline 显示剪切点

#### 第三步：手动微调剪切点

- 拖拽 SmartTimeline 上的**剪切线**调整裁切位置
- 拖拽时实时更新预览画面
- 每次拖拽前自动记录撤销状态（最多50步）
- 可点击 **"手动修正剪切点"** 按钮展开/隐藏时间线

#### 第四步：设置输出参数

| 参数 | 说明 |
|------|------|
| **尺寸适配** | 5种模式（见下表） |
| **统一命名** | 留空用原名，填写则所有导出文件统一前缀 |
| **替换尾页** | 选择一个视频文件，所有导出视频的尾页统一替换为该文件 |

**尺寸适配模式**：

| 选项 | 渲染路径 | 说明 |
|------|---------|------|
| 保持原样（极速模式） | fast_remux | FFmpeg 直出，不重编码，速度最快 |
| 适应 9:16（竖屏） | full_render | MoviePy 渲染，模糊填充/中心裁剪 |
| 适应 4:5（社交媒体） | full_render | 同上 |
| 适应 1:1（正方形） | full_render | 同上 |
| 适应 16:9（横屏） | full_render | 同上 |

#### 第五步：导出

- **仅导出当前选中**：只导出当前高亮的视频
- **同步当前比例到所有视频**：将当前行的剪切比例应用到所有行
- **一键批量导出队列所有视频**：批量导出所有勾选的视频
- 导出期间 UI 锁定（按钮禁用），显示进度条
- 随时可点击 **"停止/取消输出"** 终止导出

### 6.3 渲染引擎说明

#### fast_remux_process（极速模式）

当尺寸适配为"保持原样"时使用，FFmpeg 直接裁剪转码：

- 无尾页：单步 ffmpeg -ss -to -i 裁剪
- 有尾页：主视频转码 → 尾页转码 → concat 拼接
- 进度监控：解析 ffmpeg 的 `time=` 输出行

#### full_render_process（全量渲染）

当需要改变尺寸时使用，MoviePy 渲染：

- **模糊填充（blur）**：1/60 缩放极致模糊 + 0.4 亮度压暗作为背景，原视频居中
- **中心裁剪（crop）**：裁剪视频中心区域适配目标比例
- **等比缩放（resize）**：比例一致时仅缩放
- 支持尾页拼接（处理 mask 和音频对齐）
- CancelableLogger 继承 MoviePyProgressListener，支持用户取消

### 6.4 撤销系统

- 每次修改前自动调用 `save_state()` 记录当前任务列表和 UI 状态
- 最多保留 50 步历史
- Ctrl+Z 触发 `undo()` 恢复上一状态
- 任务运行期间不记录状态

---

## 7. 模块二：图片处理

### 7.1 界面布局

```
┌──────────────────────────────────────────────────────────┐
│ ┌─ 左侧（图片队列）─────┐  ┌─ 右侧（预览 + 参数）──────┐ │
│ │ 筛选: [按尺寸▼]       │  │  ┌────────┐ ┌────────┐    │ │
│ │ ☑ │ 文件名 │ 原尺寸    │  │  │ Before │ │ After  │    │ │
│ │ ☑ │ 1.png │1920x1080 │  │  │  原图   │ │ 预览   │    │ │
│ │ ☐ │ 2.jpg │750x1334  │  │  └────────┘ └────────┘    │ │
│ │                         │  │                          │ │
│ │ [全选/反选] [删除选中]   │  │  目标尺寸: W[___] H[___]  │ │
│ │ [清空列表]              │  │  变换模式: ○模糊填充       │ │
│ │                         │  │           ○强制拉伸       │ │
│ │                         │  │           ○等比裁切       │ │
│ │                         │  │                          │ │
│ │                         │  │  导出格式: [原格式▼]      │ │
│ │                         │  │  压缩质量: [滑块]         │ │
│ │                         │  │  [仅导出当前] [批量处理]   │ │
│ └─────────────────────────┘  └──────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

### 7.2 完整操作流程

#### 第一步：导入图片

- **拖拽导入**：将图片拖入左侧区域
- **按钮导入**：点击选择文件
- 支持格式：JPG、PNG、BMP 等常见图片格式
- 自动过滤重复图片（基于文件路径）
- 表格显示：勾选框 | 文件名 | 原始尺寸

#### 第二步：筛选

- 顶部下拉筛选器按原始尺寸分类
- 隐藏不符合筛选条件的行（不删除数据）

#### 第三步：设置变换参数

**三种变换策略**：

| 模式 | 效果 | 适用场景 |
|------|------|---------|
| **模糊填充** | 等比缩放居中，空白区域填充模糊背景 | 信息流封面，保留完整画面 |
| **强制拉伸** | 直接拉伸到目标尺寸 | 精确匹配尺寸要求 |
| **等比裁切** | 等比缩放后裁切多余部分填满画面 | 需要满屏无留白的场景 |

**目标尺寸**：
- 输入框直接填写宽高像素
- 快捷预设按钮（如 1080×1920 等）

#### 第四步：预览

- 选中图片后，右侧 Before/After 对比预览
- 修改参数后 300ms 防抖自动刷新预览
- 预览失败时日志提示错误

#### 第五步：导出

- **仅导出当前选中图片**：单张导出
- **一键批量处理所有勾选图片**：批量导出
- 支持选择导出格式：原格式 / JPG / PNG
- JPG 压缩质量滑块可调

---

## 8. 模块三：视频混剪

### 8.1 界面布局

```
┌──────────────────────────────────────────────────────────┐
│ ┌─ 左侧（素材库）─────┐  ┌─ 中间（预览）┐  ┌─ 右侧──┐  │
│ │ [随机][开头][中间]   │  │              │  │ 输出配置│  │
│ │ [结尾] Tab 切换      │  │  视频预览     │  │        │  │
│ │                     │  │              │  │ 混剪模式│  │
│ │ 素材列表:            │  │ ──Timeline── │  │ [A-E]  │  │
│ │  clip1.mp4 0-30s    │  │  播放控制     │  │        │  │
│ │  clip2.mp4 0-15s    │  │              │  │ 生成数量│  │
│ │  clip3.mp4 5-20s    │  │              │  │ 尺寸    │  │
│ │                     │  │              │  │ 音频    │  │
│ │ [删除选中] [清空]    │  │              │  │ 命名模板│  │
│ └─────────────────────┘  └──────────────┘  │        │  │
│                                              │ [开始]  │  │
│                                              │ [停止]  │  │
│                                              └────────┘  │
└──────────────────────────────────────────────────────────┘
```

### 8.2 素材库管理

**4个素材分类 Tab**：

| Tab | 类型 | 说明 | 限制 |
|-----|------|------|------|
| 随机素材 | random | 每次从中随机抽取 | 可放多条，重复利用 |
| 固定开头 | head | 每条视频以此开头 | 只能放 1 条素材 |
| 固定中间 | mid | 每条视频以此做中间段 | 只能放 1 条素材 |
| 固定结尾 | tail | 每条视频以此结尾 | 只能放 1 条素材 |

**导入方式**：
- 拖拽视频到对应 Tab 区域
- 点击 Tab 内的导入按钮选择文件

**素材区间设定**：
- 每条素材可设定可用区间 [start, end]
- 拖拽 RangeWidget 的滑块调整起止时间
- 混剪时只使用指定区间内的片段

### 8.3 混剪模式

5种预设模式，每种定义了不同的时间槽（slot）分配策略：

| 模式 | 结构 | 说明 |
|------|------|------|
| **A** | 固定开头 + 随机填充 + 固定结尾 | 三段式，开头结尾固定 |
| **B** | 随机填充 + 固定结尾 | 无固定开头 |
| **C** | 固定开头 + 随机填充 | 无固定结尾 |
| **D** | 全随机填充 | 纯随机组合 |
| **E** | 固定开头 + 固定中间 + 固定结尾 + 随机填充 | 四段式 |

> 选择模式后，不可用的 Tab 会自动禁用（灰色），防止配置冲突。

### 8.4 输出配置

| 参数 | 说明 |
|------|------|
| **生成数量** | 要生成多少条混剪视频 |
| **输出尺寸** | 预设（9:16 / 4:5 / 1:1 / 16:9）或自定义宽高 |
| **替换音频** | 选择 mp3/wav/aac 文件替换原视频音轨 |
| **命名模板** | 支持 `{index}` 和 `{date}` 变量替换 |

**素材充足性分析**：
- 自动计算当前素材是否足够生成目标数量的视频
- 不足时提示建议补充多少条随机素材
- 每条素材最多重复使用次数默认为 2

### 8.5 任务生成与渲染

**防重复策略**（三级 fallback）：

1. **优先**：从全局未使用的起点中选取
2. **放宽**：使用最少被使用的素材片段
3. **兜底**：随机选取（offset_step=1.0 生成候选起点）

**渲染流程**（三步）：

1. FFmpeg 截取各段素材的指定区间
2. FFmpeg concat 拼接所有片段
3. 替换音频（如有）/ 补静音（如原视频无声）
4. 自动清理临时文件

**进度显示**：日志区实时输出渲染进度，如 `[1/10] 渲染: video_001.mp4`

---

## 9. 快捷键与操作技巧

| 操作 | 方式 |
|------|------|
| **撤销** | Ctrl+Z（视频模块，最多50步） |
| **导入文件** | 拖拽到窗口/模块区域 |
| **右键菜单** | 视频表格中右键行 → "重新智能分析此视频" |
| **预览播放** | 视频模块：点击行即可预览；混剪模块：播放控制按钮 |
| **全选/反选** | 点击"全选/反选"按钮快速勾选 |
| **停止导出** | 点击红色"停止/取消输出"按钮，终止当前 ffmpeg 子进程 |
| **键盘控制播放** | 混剪模块：空格播放/暂停，左右方向键步进 |

---

## 10. 常见问题排查

### 启动问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 双击 EXE 无反应 | 首次启动解压慢 | 等待 30 秒 |
| "授权已到期"弹窗 | 超过硬编码到期日 | 修改 main.py 中的 expire_date |
| 闪退无提示 | 未知异常 | 检查 dist/log/ 下的日志文件 |

### 视频处理问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| AI 解析失败 | 视频编码不支持或文件损坏 | 确认视频可正常播放后重试 |
| 导出视频无声音 | FFmpeg 音频流处理异常 | 尝试"保持原样"模式 |
| 导出速度慢 | 使用了全量渲染模式 | 如不需要改变尺寸，选"保持原样"极速模式 |
| 停止导出后文件残留 | 正常行为，未完成文件已自动清理 | 检查输出目录是否有多余文件手动删除 |
| 进度条卡住 | FFmpeg 进度输出格式变化 | 耐心等待，不影响实际导出 |

### 图片处理问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 预览失败 | 图片格式不支持 | 转为 JPG/PNG 后重试 |
| 导出图片模糊 | 压缩质量设置过低 | 提高质量滑块或使用 PNG 格式 |

### 混剪问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| "素材不足"提示 | 随机素材数量不够 | 按提示补充随机素材 |
| 生成视频内容重复 | 素材池太小或生成数量过多 | 增加随机素材条数 |
| FFmpeg 未检测到 | ffmpeg.exe 缺失 | 确保 ffmpeg.exe 在项目根目录 |
| 渲染超时 | 素材过长或系统资源不足 | 缩短素材可用区间 |

---

## 11. 开发者指南

### 架构设计

```
main.py                    ← 入口（授权 + 全局异常处理）
  └── ui/main_window.py    ← 主窗口 UltimateEngine
        ├── ImageHandler   ← 图片模块（Mixin 方式继承）
        └── MixHandler     ← 混剪模块（Mixin 方式继承）

core/video_engine.py       ← 视频引擎（独立进程，通过 PyQt Signal 通信）
core/mix_engine.py         ← 混剪引擎（独立 QThread，通过 Signal 通信）
utils/ffmpeg_utils.py      ← FFmpeg 路径工具
```

### 多线程架构

| 线程类 | 所在文件 | 职责 |
|--------|---------|------|
| VideoImportThread | main_window.py | 视频导入（获取时长/分辨率等基础信息） |
| SingleAnalysisThread | main_window.py | 单视频 AI 断点分析 |
| BatchAnalysisThread | main_window.py | 批量 AI 断点分析（ThreadPoolExecutor 并发） |
| VideoExportThread | main_window.py | 视频导出（调用 VideoProcessor） |
| ImageImportThread | image_handler.py | 图片导入（获取尺寸/文件大小） |
| ImageExportThread | image_handler.py | 图片导出 |
| MixWorkerThread | mix_handler.py | 混剪渲染（调用 FFmpegMixer） |

### 信号通信

所有线程通过 PyQt Signal 与主线程通信：
- `log_signal` → 日志/状态更新
- `finished` → 任务完成通知
- VideoExportThread 通过 `self.current_out_path` 记录当前输出路径（取消时用于清理）

### 添加新功能检查清单

1. 在 `core/` 下添加引擎逻辑（纯 Python，不依赖 PyQt）
2. 在 `ui/` 下添加 Handler（继承方式或独立类）
3. 在 `main_window.py` 的 `build_*_module()` 中添加 UI
4. 在 `main.spec` 的 `hiddenimports` 中添加新模块
5. 线程操作必须通过 Signal 通信，禁止直接操作 UI 控件

### 打包前清理

```bash
# 删除构建缓存
rmdir /s /q build
rmdir /s /q dist
rmdir /s /q __pycache__
rmdir /s /q core\__pycache__
rmdir /s /q ui\__pycache__
rmdir /s /q utils\__pycache__

# 重新打包
python -m PyInstaller --clean main.spec
```

---

*最后更新：2026-05-19*
