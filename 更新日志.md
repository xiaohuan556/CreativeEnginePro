# 更新日志 · CreativeEnginePro 图片编辑器

> 格式：日期 — 模块 — 改动说明

---

## 2026-07-28 图片编辑器 · 画板体验修复（透明连点/独立/自由摆放/去约束框）

- **「透明」勾选框连点两下才生效 → 单击即生效**：根因是属性面板同步时误用 `project.transparent` 而非「当前激活画板」的 `transparent`，导致进入画板后勾选框初始状态与画板实际状态错位，第一次点击其实是把状态从「勾选框的 False」对齐到「画板已有的 False」（视觉无变化），第二次才真正打开。现 `_sync_props_panel` 在画板模式下同步 `active_artboard.transparent`。
- **透明可见性**：编辑模式下透明画板改用棋盘格显示（`_render_doc_composite` 内对 `ab.transparent` 画棋盘），单击「透明」即可直观看到变透明；导出仍保持纯透明。
- **每个画板独立**：透明状态本就按 `Artboard` 存储，同步修正后勾选框正确反映各自激活画板的透明值，互不串扰。
- **画板自由摆放（想挪到哪里就在哪里）**：`_ab_translate` 由「按 min 整体重居中」改为固定 `(MARGIN, MARGIN)` 平移；`_doc_bounds` 同步改为固定上下/左右边距。拖动单个画板时其它画板位置不再漂移、不再整体重排；拖拽时夹紧 `x,y>=0` 防止拖出文档左上角，并在拖拽起点 `_invalidate_doc_bounds()` 让画布随画板扩大。
- **删除画板约束框**：`_draw_artboards` 不再绘制画板边框与名称标签叠加层，画板即纯内容，可在画布内任意位置摆放；激活状态由图层面板标识。

---

## 2026-07-28 视频工作台 · 字幕/主轨 5 项体验修复（残影/卡顿/关键帧/逐次淡入/边框跟随）

- **主轨二次拖动不再闪屏**：`_on_selection_changed` 仅在选中的视频片段对象真正变化时清除 `_seq_state`，重复选中同一片段不再强制重解码。
- **字幕残影根治**：引入"无字幕合成帧"缓存 `_last_frame_no_subs` 与 `_subtitle_base_canvas()` 辅助方法；`_flush_frame` 两条字幕重绘快速路径改用干净底图（先重绘其它活跃字幕，再绘当前选中字幕+选中框），彻底杜绝旧字幕叠加残影；字幕关键帧因此可见生效。
- **新建字幕也可用「逐次淡入」**：`_overlay_subtitles` 去掉 `from_asr` 渲染门控，非 ASR（手打）字幕勾选逐词动画后正常逐词淡入（无时间戳自动等分）。
- **字间距边框跟随**：`_compute_sub_bbox` 计算选中框宽度时计入 `letter_spacing`（每行 `+letter_sp*(len-1)`），与渲染一致，字间距拉大时边框同步变宽。
- **字幕变换不再卡顿**：字幕属性（位置/缩放/旋转/字间距等）变更走轻量 `_recompose_overlays()` 重绘叠加层，不再 `seek(force=True)` 全量重解码；视频/音频属性仍走原路径。
- **主轨拖动"来回跳"修复**：`_on_screen_move` 的视频拖拽/缩放/旋转分支原每像素调用 `_set_seq_state(None)` 强制解码器重新 seek，播放中拖动会导致解码器短暂返回错帧 → 画面来回跳。改为仅在 press/release 一次性清 `_seq_state`，拖动中复用快速路径（从缓存帧 `_last_raw_img` 重定位），消除跳跃。
- 验证：`py_compile` 全部通过。
- 备注：第2/3/4 项（字幕残影、字间距边框、逐次淡入）的源码修复已在上一轮落地（引入无字幕底图 `_last_frame_no_subs` + `_subtitle_base_canvas`、字幕 bbox 计入字间距、去掉 `from_asr` 渲染门控）。若运行编译版 exe 仍见旧行为，需用 `python main.py` 从源码运行或重新打包，源码改动不会自动反映到旧 exe。

## 2026-07-24 图片编辑器 · AI 超清放大体验 + 稳定性加固

### ✨ 新增：超清放大「前后对比」按钮
- 顶栏新增 **「◑ 前后对比」** 开关按钮（默认禁用，跑完一次高清放大后自动启用）。
- 点击在 **「原图（放大前）↔ AI 结果」** 之间切换；画布上叠加原图时会显示蓝色「原图（放大前）」角标。
- 实现：`_ai_upscale` 在起点记录 `self._compare_before`（放大前像素），`_on_compare_toggle` 切换 `self._compare_active`，`_redraw` 经 `_draw_compare_overlay` 在原图层画布包围盒上绘制原图。画板模式下自动跳过，避免错位。

### 🕑 新增：历史记录「原图」快照（可一步回溯）
- 之前高清放大是"一步到位"，历史里只有放大后状态，无法回到原图。
- 现在 `_on_ai_enh_done` 在**写入放大结果之前**先 `push_undo("原图（放大前）")`，完成时再 `push_undo("AI 高清放大 N×")`。
- 历史面板会出现独立的「原图（放大前）」步骤，点击即可回溯到放大前。

### ⚠️ 改进：AI 模型失败不再"静默装成功"
- 之前若 Real-ESRGAN 模型未下载/推理失败，会静默降级为 Lanczos+USM（与原图几乎无差别），弹窗却写"高清放大完成"，让人误以为 AI 生效了。
- 现在若 engine 含 `Lanczos`/`未启用`，完成弹窗改为 **warning**，明确告知"AI 模型未启用，已用传统放大"，并指引去「🤖 AI → 📦 模型管理」下载 Real-ESRGAN。
- 成功时提示"点「◑ 前后对比」可对比原图"。
- 注：Real-ESRGAN 模型直链（hf-mirror / Heliosoph `realesr-general-x4v3.onnx`）已验证仍可达。

### 🛡️ 加固：BiRefNet 去背健壮性
- `qimage_from_numpy` 改为**防御式**：强制连续内存、非 uint8 自动截断、2D/单通道/3 通道自动补齐为 RGBA，杜绝非法缓冲导致的 `wrapped C/C++ object of type QImage has been deleted` 诡异崩溃。
- `_run_birefnet` 输出**形状容错**：`np.squeeze` 统一压成 (H,W)，形状异常抛清晰中文错误；裁剪前校验尺寸避免越界。
- `_run_onnx_worker` 失败时把子进程 **stderr 透出**到 RuntimeError 文本，便于定位真实 onnx 错误。

### 🐞 新增：全局未捕获异常处理器（`main.py`）
- 新增 `sys.excepthook`：捕获 PyQt 槽函数 / 渲染中的未处理异常，写入 `~/.cep_models/cep_crash.log` 并弹窗显示末尾 3000 字符。
- 置于 `--ai-worker` / `--realesr-worker` 子进程 `sys.exit(0)` 之后，子进程不受影响。
- 作用：下次若再出现 BiRefNet "QImage has been deleted" 之类诡异崩溃，可直接拿到完整 traceback 定位。

### 🔧 关键修复：BiRefNet / CodeFormer 在**源码模式**仍报 `onnxruntime DLL load failed`
- **现象**：模型已下载（938.4MB ✅），但 AI 智能抠图报错
  `ImportError: DLL load failed while importing onnxruntime_pybind11_state`。
- **根因**：原 `_run_birefnet` / `_run_codeformer` 用 `if getattr(sys, "frozen", False)` 分流——
  只在 **打包 EXE** 时走 `--ai-worker` 干净子进程，而**源码模式（`python main.py`，`frozen=False`）**
  直接在**主进程内 `import onnxruntime`**。此时主进程早已加载 PyQt6/cv2，其 C++ 运行库与 onnxruntime
  原生 DLL（`onnxruntime_pybind11_state`）抢占冲突，导致 DLL 初始化失败。
  用户正是源码模式运行，所以之前 EXE 专用子进程方案没覆盖到他。
- **修复**：删除 `sys.frozen` 分支，**无论 EXE 还是源码，统一走 `_run_onnx_worker` 干净子进程推理**
  （`main.py --ai-worker`，在 import PyQt6/cv2 之前 `sys.exit(0)`，子进程内 onnxruntime 可正常加载）。
- **验证**：`python -c "import onnxruntime"` 在干净进程返回 `1.27.0`；`main.py` + `ui/image_editor.py` `py_compile` 通过。

---

## 2026-07-24 图片编辑器 · 画板外观与布局改造

### 🎨 画板背景统一实色（取消透明棋盘格）
- `_render_doc_composite`（编辑模式）画板外区域由 `_paint_checker` 改为 **实色深灰 `#1a1a1a`**，导出（`for_export`）时仍保持透明。
- 每个 `Artboard` 默认 `transparent=False` + 白底；新建画板显式 `transparent=False`，**从首画板起全部实色底**，不再出现透明棋盘格。

### 📐 画板自动横向排列
- `_add_artboard` 原「3 列网格布局」改为 **单行横向排列**：新画板自动排到所有已有画板最右侧（`nx = Σab.w + gap·N`，`ny=0`，`gap=80`），顶部对齐，无需手动摆放。

### 🖱️ 画板自由拖动（任意画板一拖即走）
- 原逻辑：点非激活画板只切换激活、不拖拽；只有已激活画板能拖。
- 现改为：`CanvasView.mousePressEvent` 中 **点中任意画板先激活再直接拖拽**（`_artboard_drag`），一次手势即可拖动**任意**画板；`mouseMove` 更新 `ab.x/ab.y` + `_redraw`，`mouseRelease` 结束。

---

## 2026-07-23 图片编辑器 · AI 模型与抠图问题修复（背景）

- **GFPGAN 下载失败**：原 URL 404，替换为 hacksider/deep-live-cam 直链 + hf-mirror 备用（已验证 200 / 340MB）。
- **BiRefNet / CodeFormer 在 EXE 内 `onnxruntime DLL load failed`**：主进程已加载 PyQt6/cv2 后 import onnxruntime 冲突。新增 `main.py --ai-worker` 干净子进程分支，推理在子进程完成，主进程只做 cv2/numpy 预处理与后处理。
- **AI 抠图每次弹"当前导入错误"**：`_ensure_rembg` 导入失败先静默探测子进程，可用则直接启用子进程模式不再弹窗，并加 `_rembg_ensured` 缓存避免重复提示。
- 基础图片编辑功能补齐（蒙版绘制、色阶/曲线/白平衡/色彩平衡、一键旋转、渐变工具、克隆/修复/内容识别、对齐分布、历史面板、调整图层、导出 JPG/WebP）。

---

## 验证情况
- `main.py` + `ui/image_editor.py` 均 `py_compile` 通过。
- BiRefNet 输出后处理数学（sigmoid→matte→裁剪→缩放→RGBA 拼合）冒烟测试通过。
- Real-ESRGAN 与 BiRefNet 模型下载直链均经 HEAD 验证可达。
- 提示：若以 **EXE（打包）** 运行，`main.py` 的 `--ai-worker` 改动需**重新打包**才会生效；源码模式（`python main.py`）直接生效。

---

## 2026-07-28 视频工作台 · 主轨关键帧「二次拖动」闪烁/跳跃修复
- **现象**：主轨片段带关键帧时，第一次拖动正常，第二次（及之后）拖动画面来回跳变、闪烁。
- **根因**：拖拽中 8ms 定时器不断调用 `_flush_frame()`（无 force）。当 `_pending_raw`/帧缓存环可用时 `need_frame=True`，落入「完整合成」路径，主视频按**关键帧插值位置**渲染，与拖拽实时位置打架 → 跳变。首次拖动无关键帧（插值返回基值==拖拽值）故不显；首拖后关键帧已同步，二次拖即暴露。
- **修复**：`_flush_frame` 快速路径的进入条件由「仅 force」放宽为「force **或** 处于交互态（`_dragging_video`/`_resize_handle`/`_rotation_active`/`_sub_interaction`）」。这样交互期间定时器也走快速路径（`_compose_from_raw` 对正在交互片段**跳过关键帧插值**，用真实拖拽基值），彻底杜绝跳变；完整合成路径仅在交互结束后（释放、刷新帧）才执行。
- 验证：`py_compile ui/preview_player.py` 通过。

---

## 2026-07-28 图片编辑器 · 画板"深色约束框"澄清修复
- **背景**：上一轮误把"画板边框+名称标签"当成约束框删掉了；用户澄清——约束框是**画板底部那块深色背景**，它会在画板往下挪时裁切内容。
- **修复1（裁切根因）**：画板拖拽 move 时只更新 ab.x/ab.y 并 `_redraw()`，但 `_doc_bounds()` 缓存未失效 → 场景矩形停留在拖拽前尺寸，画板下移被深色页面裁切。现 move 中每次 `_invalidate_doc_bounds()`，画布随画板实时扩大，不再裁切。
- **修复2（恢复边框+名称）**：`_draw_artboards` 还原为绘制每画板边框线 + 顶部名称标签框（激活态青色 #00eaff，否则灰），纯叠加层不进入导出。
- **修复3（去掉深色"约束框"）**：`_render_doc_composite` 编辑/导出均改透明底，不再填实色深灰 #1a1a1a 页面；画板悬浮在视图中性背景上，彻底不再有"底部深色约束框"。
- 验证：py_compile ui/image_editor.py 通过。

---

## 2026-07-28 图片编辑器 · 吸管/渐变工具编辑区指示标
- **吸管**：之前用 PointingHandCursor（手指），编辑区无专属「小标志」。现：
  ① `set_tool` 切到吸管时生成专属吸管图标光标（`_eyedropper_cursor`，青色 #00eaff，笔尖为热点）；
  ② 激活时缓存合成图，悬停移动时在光标旁实时绘制取色 HUD 小圆（新增 `_update_eyedrop_preview` + `CanvasView._paint_eyedrop_hud`，鼠标离开画布 `leaveEvent` 清除），圆内填充当前像素颜色，透明/画布外画灰底+红斜杠。
- **渐变**：之前用 CrossCursor 且无拖拽预览。现：
  ① `set_tool` 切到渐变时生成专属「线段」光标（`_gradient_cursor`，起止色圆点+青色连线）；
  ② `mouseMoveEvent` 拖拽时实时显示**线段指示**（`_preview_gradient`：起点/终点圆点填充起止渐变色 + 青色连线 + 角度/「径向」标注），让用户直观看到渐变方向与范围；
  ③ `mouseReleaseEvent` 创建渐变图层后清除预览线段。（注：渐变拖拽本就因 press 默认 `_dragging=True` 可用，本次补的是「线段显示」。）
- 验证：`py_compile ui/image_editor.py` 通过。

---

## 2026-07-28 图片编辑器 · 吸管/渐变崩溃热修
- **现象**：使用吸管/渐变工具时崩溃：`AttributeError: 'CanvasView' object has no attribute 'view'`，定位在 `CanvasView.mousePressEvent` / `mouseReleaseEvent` 的渐变分支。
- **根因**：在 `CanvasView`（视图本身）内部误写了 `self.view.sel_item.setPixmap(...)`，但 `CanvasView` 没有 `self.view` 属性，应直接访问 `self.sel_item`。
- **修复**：将 line 655（press）和 line 878（release）的 `self.view.sel_item` 改为 `self.sel_item`。
- 验证：`py_compile ui/image_editor.py` 通过。
