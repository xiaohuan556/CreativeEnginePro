# AI 资源中心 Tab 说明（V2 · 资产浏览器）

> 模块路径：`ai/ui/resource_center.py`
> 挂载位置：主窗口左侧导航栏「🤖 AI 资源中心」按钮 → `UltimateEngine.stacked` 的 **index 8**
> 数据存储：`~/.cep_data/ai_assets.db`（SQLite，由 `ai.service.get_asset_db()` 管理）

---

## 1. 设计定位：Asset Browser，而非数据库表单

V2 彻底推翻 V1 的「SQLite 表单编辑器」思路，按 **Adobe Bridge / Lightroom / Premiere Project 面板 / ComfyUI 模型浏览器** 的范式重做：

- 左侧是**资源浏览器**（缩略图卡片网格 + 搜索），不再是空黑边
- 右侧是**资源详情 + 预览面板**，资源「活」起来
- 四类资源各有 **Accent 色**，一眼知道自己在哪
- Prompt 默认**折叠**，页面清爽；卡片化，可承载上千资源

---

## 2. 界面结构

```
┌──────────────────────────────────────────────────────────┐
│  🗂 AI 资源中心（标题随类别变 Accent 色）                  │
├──────────────────────────────────────────────────────────┤
│  [👤 人物] [🏞 场景] [📝 Prompt] [🎤 声音]  （类别切换） │
├──────────────────────┬───────────────────────────────────┤
│  左侧 资源浏览器 30%  │  右侧 资源详情 + 预览 70%         │
│  🔍 搜索              │  ┌─ 预览（参考图 / 试听 / 模板） │
│  ┌────┐ ┌────┐       │  ├─ 基本信息                      │
│  │卡①│ │卡②│ ...     │  ├─ ▼ Seedream/Veo Prompt（折叠）│
│  └────┘ └────┘       │  ├─ ▼ 高级设置（折叠）            │
│  +新建 复制 删除      │  └─                   💾 保存      │
└──────────────────────┴───────────────────────────────────┘
```

---

## 3. 四类资源的 Accent 色

| 类别 | 按钮 | Accent |
|------|------|--------|
| 人物 | 👤 人物 | 紫 `#b98cff` |
| 场景 | 🏞 场景 | 绿 `#48d597` |
| Prompt | 📝 Prompt | 橙 `#ffa53d` |
| 声音 | 🎤 声音 | 蓝 `#4aa3ff` |

标题栏、类别选中下划线、卡片选中边框、保存按钮、预览试听按钮均使用当前类别的 Accent。

---

## 4. 左侧：资源浏览器（CategoryBrowser）

- **搜索框**（顶部）：按「名称 + 标签」实时过滤，不区分大小写
- **卡片网格**（中间）：`ResourceCard` 缩略图卡片
  - 人物/场景：自动取 `reference_images[0]` 做封面裁剪缩略图；无图则用 Accent 色占位 + 类别图标（👤/🏞）
  - 卡片显示：缩略图 + 名称 + 元信息（人物：`年龄·性别·N标签`；场景：标签；声音：模型）
  - 列数随容器宽度自适应（`宽度 // 160`）
  - 选中卡片有 Accent 色边框
- **操作按钮**（底部，资源级）：`＋ 新建` / `⧉ 复制` / `🗑 删除`
  - 新建：清空右侧进入编辑态
  - 复制：克隆当前项（新 id + 名称加「 (副本)」）
  - 删除：立即删除（无二次确认）

点选卡片 → 右侧加载该项详情。

---

## 5. 右侧：资源详情 + 预览（DetailPanel）

### 5.1 预览面板（顶部，按类别不同）
- **人物 / 场景**：参考图缩略图横排（最多 6 张），点缩略图用系统查看器打开原图
- **声音**：`▶ 试听` 按钮（QMediaPlayer 播放 `sample_path`，失败回退系统播放器）+ 模型/voice_id 信息
- **Prompt**：只读「模板预览」文本框，随「模板」编辑**实时更新**

### 5.2 基本信息（中间表单）
| 类别 | 字段 |
|------|------|
| 人物 | 名称 / 年龄 / 性别 / 描述 |
| 场景 | 名称 / 描述 |
| Prompt | 名称 / **适用于**（Seedream·Veo·GPT Image·Kling·FLUX 复选框）/ 分类 / 变量(逗号) / 模板 |
| 声音 | 名称 / 模型(Provider) / voice_id / 参考音频路径 |

### 5.3 折叠区
- **▼ Seedream / Veo Prompt**（人物/场景）：默认折叠，展开后编辑 seedream_prompt / veo_prompt
- **▼ 高级设置**：默认折叠
  - 人物：参考图路径(列表) / embedding·LoRA 路径 / 标签
  - 场景：参考图路径 / 标签
  - Prompt：标签 / 默认参数(JSON，除 vars)
  - 声音：标签

### 5.4 保存（编辑级，右下角）
- 仅 `💾 保存` 一个按钮，右对齐
- 保存时若 id 为空则生成 `uuid4`；保留 `created_at`，刷新 `updated_at`
- 保存后浏览器列表刷新并重新选中该项

---

## 6. 数据与字段映射

存储层不变（`ai.assets`）：`Character` / `Scene` / `PromptTemplate` / `VoicePreset`，四张 SQLite 表。

UI → 字段映射要点：
- Prompt「适用于」复选框 → `provider`（逗号串）
- Prompt「变量」→ `defaults["vars"]`（列表）
- 参考图 / 标签 → `reference_images` / `tags`（逗号解析为列表）
- 列表读取上限 `limit=5000`，支持未来上千资源

---

## 7. 扩展方式

新增一类资源：
1. `ai/assets/db.py` 增加数据类 + 表 + `list/save/delete/get` 方法
2. `resource_center.py` 的 `KIND_ORDER` / `KIND_META`（含 accent/glyph）/ `DB_MAP` 增加条目
3. `DetailPanel._build_basic` / `_build_advanced` 增加该类的字段分支
4. 如需特殊预览，在 `_build_preview` 增加分支

`ResourceCard` / `CategoryBrowser` / `DetailPanel` / `Collapsible` 为通用组件，按 `kind` 元配置渲染，无需改动。

---

## 8. 已知边界

- 参考图 / 音频仅存**路径字符串**，浏览器不拷贝或校验文件是否存在（缺失则显示 Accent 占位）
- 列表为一次性加载（非虚拟滚动），上千资源时建议后续加分页/懒加载
- 删除无二次确认，依赖 SQLite 文件本身备份
- Prompt「适用于」复选写入 `provider` 字段，与旧「provider 为单模型名」约定不同，旧数据首次编辑后会被逗号串覆盖（符合新设计）
