# CreativeEnginePro 正式发版

## 本版授权规则

- 正式版不开放注册，只允许服务端管理员创建并启用的账号登录。
- 正式构建默认在北京时间 **2026 年 12 月 2 日 00:00** 停止运行，即 12 月 1 日全天仍可使用。
- 启动时同时检查本机时间和登录服务器返回的可信时间，修改电脑时间不能延长使用期。
- 应用运行期间每 10 分钟复查账号；管理员停用账号、重置密码或强制下线后，桌面端会退出。
- 正式登录地址只在构建时写入 EXE，接收者不能通过 `.env` 更换认证服务器。
- 登录后，桌面端只显示该账号被管理员批准的模型；付费图片、视频、LLM 和语音任务通过中心 API/Worker 执行。

## 账号由管理员控制

服务端已有完整账号系统，不要把账号密码或模型 API Key 写进 EXE：

1. 按 `server/DEPLOYMENT.md` 部署服务端和网页管理端，正式环境使用 HTTPS 与 PostgreSQL。
2. 在服务器运行 `python server/scripts/create_admin.py` 创建你的管理员账号。
3. 登录网页管理端，在“公司账号管理”中创建、批准、停用账号，配置角色、模型白名单和额度。
4. 需要立即踢下线时，使用账号管理里的“强制下线”。
5. 模型 API Key 只写入服务器的 `deploy/.env.production`，由 Worker 使用；桌面 EXE 不包含密钥。

## 生成正式 EXE

在安装好项目依赖和 PyInstaller 的 64 位 Python 环境中运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\build_release.ps1 `
  -AuthBaseUrl "https://你的正式域名"
```

如果 Python 不在 PATH：

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\build_release.ps1 `
  -AuthBaseUrl "https://你的正式域名" `
  -PythonExe "C:\完整路径\python.exe"
```

脚本会先运行全部测试，再生成：

- `dist/CreativeEnginePro.exe`
- `dist/CreativeEnginePro.sha256.txt`

构建时不会打入仓库 `.env` 或任何 API Key。桌面端会随包包含中心 API
客户端，但中心 API、PostgreSQL、Worker 和账号管理网页应部署在你控制的服务器，
不能放进用户 EXE；否则每台电脑都会得到独立账号库，无法统一停用和限额。

为避免 Torch 原生 DLL 冲突和数 GB 体积，单文件版不内嵌本地 Whisper/Torch；
转写应使用已配置的在线服务，或另行提供独立的本地模型服务。

## 生成安装包

安装 Inno Setup 6 后运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\build_installer.ps1
```

安装包默认按用户安装，无需管理员权限。用户配置、画布状态和缓存写入
`%LOCALAPPDATA%\CreativeEnginePro`，卸载或升级程序不会因 PyInstaller 临时目录而丢失。

## 发给别人之前必须验证

1. 在没有 Python 的干净 Windows 10/11 x64 电脑安装并启动。
2. 验证错误密码、停用账号和强制下线。
3. 把系统日期改早，确认服务端时间仍会拦截过期版本。
4. 验证 AI 制片画布、Seedance 2.0/2.5、按时间戳修改、图片批量、手绘、音频克隆和视频导出。
5. 用代码签名证书签名 EXE 和安装包，降低 Windows SmartScreen 拦截概率。
