# ═══════════════ CreativeEnginePro - 统一依赖 ═══════════════
# 融合: 图片/视频处理 + 图片轮播 + 热点雷达 + TTS语音配音 + 视频译制 + AI脚本

# ── GUI 框架 ──
PyQt6>=6.5.0
# QtMultimedia / QtMultimediaWidgets are included in the PyQt6 distribution.

# ── TTS 语音引擎 ──
edge-tts>=6.1.0
requests>=2.31.0
Pillow>=10.0.0

# ── AI / ASR ──
openai>=1.30.0
openai-whisper>=20231117

# ── 翻译 ──
deep-translator>=1.11.0

# ── 音频处理 ──
pydub>=0.25.1
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0

# ── 视频处理 ──
moviepy>=1.0.3
# TikTok 2026 网页挑战需要 curl_cffi 的浏览器 TLS 伪装；default 额外项
# 同时补齐 yt-dlp 的常用加密、压缩与元数据依赖。
yt-dlp[default,curl-cffi]>=2026.8.19
# opencv-contrib-python 是 opencv-python 的超集，提供 FaceDetectorYN（人脸修复检测所需）
opencv-contrib-python>=4.8.0

# ── 音频分离 (按需安装，较重) ──
# torch>=2.0.0
# onnxruntime>=1.15.0
# spleeter>=2.4.0
# demucs>=4.0.0

# ── AI 抠图 (按需安装；程序内也可自动安装) ──
rembg>=2.0.50
onnxruntime>=1.18.0

# ── PSD 导入 (图片工作台) ──
psd-tools>=1.9.30
